(function() {
    'use strict';

    angular
        .module('app')
        .service('Service', Service);

    // Service.$inject = ['dependencies'];

    /* @ngInject */
    function Service(dependencies) {

    }
})();
